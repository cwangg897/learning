#### 인그레스
```
쿠버네티스에서 사용하는 API GateWay다

사용은 주로 Private환경에서는 Nginx를 파드로 먼저 띄우고 인그레스를 띄운다 그리고 Nginx가 인그레스를 읽어서
라우팅하게 한다

GCP같은경우 클러스터 앞에 ALB를 구성하고
클러스터내부에 인그레스를 생성하면 ALB에 인그레스 룰이적용된다
동일한것은 URI와 도메인을 가지고 분산한다.
```
